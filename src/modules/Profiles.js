const ADD = 'ADD_PROFILE'

var defaultState = require('../api/profiles')

export default function reducer(state = defaultState, action) {
    switch (action.type) {
        default:
            return state
    }
}
